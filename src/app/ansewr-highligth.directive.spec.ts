import { AnsewrHighligthDirective } from './ansewr-highligth.directive';

describe('AnsewrHighligthDirective', () => {
  it('should create an instance', () => {
    const directive = new AnsewrHighligthDirective();
    expect(directive).toBeTruthy();
  });
});
